# Credit-Risk-Classification
Supervised ML pipeline for loan default prediction — logistic regression (Ridge/Lasso), Random Forest, PCA, and stratified k-fold cross-validation on structured lending data.
